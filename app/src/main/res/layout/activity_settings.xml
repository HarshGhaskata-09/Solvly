<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fillViewport="true"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Custom Header that scrolls with content -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="24dp">

            <ImageButton
                android:id="@+id/buttonBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_back_custom"
                app:tint="@color/on_surface" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="sans-serif-medium"
                android:text="@string/title_settings"
                android:textColor="@color/on_surface"
                android:textSize="22sp" />
        </LinearLayout>

        <!-- General Section -->
        <TextView
            style="@style/SettingsSectionTitle"
            android:text="@string/settings_section_general" />

        <com.google.android.material.card.MaterialCardView
            style="@style/SettingsCard">
            <LinearLayout style="@style/SettingsContainer">
                <include android:id="@+id/itemGeneral" layout="@layout/item_setting_row" />
                <include android:id="@+id/itemNotifications" layout="@layout/item_setting_row" />
                <include android:id="@+id/itemHistory" layout="@layout/item_setting_row" />
                <include android:id="@+id/itemClearCache" layout="@layout/item_setting_row" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Appearance Section -->
        <TextView
            style="@style/SettingsSectionTitle"
            android:text="@string/settings_section_appearance" />

        <com.google.android.material.card.MaterialCardView
            style="@style/SettingsCard">
            <LinearLayout style="@style/SettingsContainer">
                <include android:id="@+id/itemAccentColor" layout="@layout/item_setting_row" android:visibility="gone" />
                <include android:id="@+id/itemTheme" layout="@layout/item_setting_row" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Account & Workspace -->
        <TextView
            style="@style/SettingsSectionTitle"
            android:text="@string/settings_section_account" />

        <com.google.android.material.card.MaterialCardView
            style="@style/SettingsCard">
            <LinearLayout style="@style/SettingsContainer">
                <include android:id="@+id/itemSubscription" layout="@layout/item_setting_row" android:visibility="gone" />
                <include android:id="@+id/itemWorkspace" layout="@layout/item_setting_row" android:visibility="gone" />
                <include android:id="@+id/itemDataControl" layout="@layout/item_setting_row" android:visibility="gone" />
                <include android:id="@+id/itemSecurity" layout="@layout/item_setting_row" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Support & Info -->
        <TextView
            style="@style/SettingsSectionTitle"
            android:text="@string/settings_section_support" />

        <com.google.android.material.card.MaterialCardView
            style="@style/SettingsCard">
            <LinearLayout style="@style/SettingsContainer">
                <include android:id="@+id/itemReportBug" layout="@layout/item_setting_row" />
                <include android:id="@+id/itemPolicy" layout="@layout/item_setting_row" />
                <include android:id="@+id/itemLicence" layout="@layout/item_setting_row" />
                <include android:id="@+id/itemAbout" layout="@layout/item_setting_row" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="24dp">

            <TextView
                android:id="@+id/textAppVersion"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/settings_version_text"
                android:textColor="@color/text_secondary"
                android:textSize="13sp"
                android:fontFamily="sans-serif-medium"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="@string/settings_made_in_india"
                android:textColor="@color/text_secondary"
                android:textSize="14sp"
                android:fontFamily="sans-serif-medium"
                android:alpha="0.8"/>
        </LinearLayout>

    </LinearLayout>
</androidx.core.widget.NestedScrollView>
